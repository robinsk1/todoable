.row
  .large-12.columns

    %p#notice= notice

    %p
      %b Project name:
      = @project.name
    %p
      %b Description:
      = @project.description

    -if can? :manage, :project
      = link_to 'Edit', edit_project_path(@project)
