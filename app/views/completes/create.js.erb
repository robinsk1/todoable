//var opened = $(".opened").length;
//var closed = $(".closed").length;
//var percentage;
//
//if (opened > 0 && closed > 0)
//  { percentage = opened / closed * 100}
//else
//  {percentage = "hey"}
//
$('.percentage').html(percentage + "%");
//$('.percentage').effect("highlight", {}, 3000);
